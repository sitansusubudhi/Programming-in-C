#include <stdio.h>

int main() {
	enum boolean {NO, YES};
	enum months {
		     JAN = 1, FEB, MAR, APR , MAY, JUN,
		     JUL, AUG, SEP, OCT, NOV, DEC
		    };
	printf("%d\n", NO);
	printf("%d", DEC);
}
